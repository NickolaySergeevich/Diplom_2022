<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:diplomapp="clr-namespace:DiplomApp"
             x:DataType="diplomapp:AppShell"
             x:Class="DiplomApp.Views.LoginPage"
             Title="Login page">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center">
            <Grid ColumnDefinitions="Auto,*"
                  RowDefinitions="Auto,Auto,Auto"
                  Margin="10">
                <Label Text="Login:"
                       VerticalTextAlignment="Center"
                       FontSize="22" />
                <Entry x:Name="entry_login"
                       Grid.Column="1"
                       Placeholder="Введите ваш логин" />
                <Label Grid.Row="1"
                       Text="Password:"
                       VerticalTextAlignment="Center"
                       FontSize="22" />
                <Entry x:Name="editor_password"
                       Grid.Row="1"
                       Grid.Column="1"
                       Placeholder="Введите ваш пароль"
                       IsPassword="True" />
                <Button Grid.Row="2"
                        Grid.ColumnSpan="2"
                        Text="Вход"
                        Clicked="Button_Clicked" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>