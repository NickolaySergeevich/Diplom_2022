<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:diplomapp="clr-namespace:DiplomApp"
             x:DataType="diplomapp:AppShell"
             x:Class="DiplomApp.Views.ChatPage"
             Title="Chat">
    <ContentPage.Content>
        <StackLayout>
            <Grid RowDefinitions="Auto,Auto"
                  ColumnDefinitions="Auto,Auto"
                  Margin="10">
                <Label Text="Имя участника:"
                       VerticalOptions="Center"
                       FontSize="20"/>
                <Entry Grid.Column="1"
                       x:Name="entry_whatFind"
                       Placeholder="Введите имя участника" />
                <ListView Grid.Row="1"
                          Grid.ColumnSpan="2"
                          x:Name="listView_chats" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>