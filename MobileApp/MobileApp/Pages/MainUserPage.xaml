<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:pages="clr-namespace:MobileApp.Pages" x:DataType="pages:MainUserPage"
             xmlns:responses="clr-namespace:MobileApp.ApiJsonResponse"
            x:Class="MobileApp.Pages.MainUserPage">
    <ContentPage Title="Кейсы" >
        <ListView x:Name="listView_tasks">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout x:DataType="responses:TasksResponse">
                            <Label Text="{Binding Name}" FontSize="Large"></Label>
                            <Label Text="{Binding Description}" FontSize="Small"></Label>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage>
    <ContentPage Title="Tab 2" />
    <ContentPage Title="Tab 3" />
</TabbedPage>